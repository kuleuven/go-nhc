# File managed by Puppet, do not edit!
#
# NHC Configuration File
#
# Lines are in the form "<hostmask>||<check>"
# Hostmask is a glob or /regexp/
# Comments begin with '#'
#
# $Id$
#

#######################################################################
###
### NHC Configuration Variables
###

* || export MARK_OFFLINE=0
* || export MAX_SYS_UID=9000
* || export NHC_RM=pbs

#######################################################################
###
### Checks
###

* || check_ps_unauth_users log syslog die

* || check_ps_service -u root -S pbs_mom
* || check_ps_service -u root -S trqauthd
* || check_ps_service -u root -S sshd
* || check_ps_service -u polkitd -S polkitd
* || check_ps_service -S ntpd
* || check_ps_service -u root -S sssd
* || check_ps_service -u nscd -S nscd
* || check_ps_service -u root -S crond
* || check_ps_service -r -d rpc.statd nfslock
* || check_ps_service -r rpcbind
* || check_ps_service -d automount -u root -S autofs
* || check_ps_service -r -d qmgr postfix
